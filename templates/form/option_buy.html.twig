{% extends 'base.html.twig' %}

{% block title %}BSX - Dashboard{% endblock %}

{% block body %}
    {{ form(form) }}
    {% if error %}
        <br />
        {{ error }}
    {% endif %}

    <script>
        $(() => {
            $("#option_buy_average, #option_buy_contracts").on("change", function(){
                var cost = (((parseFloat($("#option_buy_average").val()) * 100) * parseInt($("#option_buy_contracts").val())) + (parseInt($("#option_buy_contracts").val())) * 1.25) + 9.95;
                $("#option_buy_cost").val(cost);
            });
        });
    </script>
{% endblock %}